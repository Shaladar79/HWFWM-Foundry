/**
 * Actor configuration wrapper.
 *
 * Other system modules should import shared Actor defaults and helpers through
 * this wrapper rather than importing individual Actor configuration files.
 */

export {
  createDefaultActorData
} from "./actor-defaults.js";
